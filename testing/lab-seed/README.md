# Seed App

Minimal app used as the recurring test target for SchwarzWorker commands.

## Files

- `src/index.js` - tiny CLI entry point
- `src/auth.js` - simple helper module

The app is intentionally small so phase plans can evolve it quickly during broad workflow tests.
